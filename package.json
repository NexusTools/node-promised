{
  "name": "@nexustools/promised",
  "description": "Extension of Promise which exposes resolve/reject methods and makes the executor optional.",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "mocha --bail --reporter spec test/",
    "test-coverage": "nyc --reporter=lcov --reporter=text-summary npm test"
  },
  "types": "index.d.ts",
  "author": "NexusTools <contact@nexustools.com>",
  "contributors": [
    "Katelyn Slater <kate@nexustools.com"
  ],
  "repository": "github:NexusTools/node-promised",
  "license": "Apache-2.0",
  "dependencies": {},
  "devDependencies": {
    "@types/mocha": "^8.2.0",
    "@types/node": "^14.14.22",
    "mocha": "^8.2.1",
    "nyc": "^15.1.0",
    "source-map-support": "^0.5.19"
  }
}
